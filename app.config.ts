export default defineAppConfig({
  ui: {
    // Cores semânticas - Esquema Laranja do Clube da Pizza
    colors: {
      primary: 'orange',    // Laranja #f97316 - Cor principal (botões, CTAs)
      secondary: 'red',     // Vermelho #ef4444 - Cor secundária (preços, destaques)
      success: 'orange',    // Laranja também para success (sem verde no esquema)
      warning: 'yellow',    // Amarelo - Badges "POPULAR" e avisos
      error: 'red',         // Vermelho para erros
      neutral: 'slate'      // Cinza - Elementos neutros
    }
  }
})

